# Responsible Machine Learning – Homework 1

# Purpose 

The goal of this assignment was to take the Lecture 01 R workflow and translate it into Python using the COMPAS dataset. I followed the same overall steps as the original script, including cleaning the data, exploring it, building a logistic regression model, and evaluating the results.

This project shows that even though R and Python use different syntax, the same machine learning process can be recreated and lead to similar insights.

# Python Libraries Used

I used the following Python libraries:

- pandas for loading and working with the dataset  
- numpy for basic numerical operations  
- matplotlib for simple visualizations  
- scikit-learn for splitting the data, building the logistic regression model, and evaluating performance  

# Instructions for Reproducing the Results

1. Open the notebook file (`.ipynb`) in this repository.
2. Make sure the required libraries are installed (pandas, numpy, matplotlib, scikit-learn).
3. Run all cells from the top to bottom.
5. Run

---

# Notes

Some of the results in Python may be slightly different from the R version due to differences in how the libraries work, but the overall process and conclusions are the same.
